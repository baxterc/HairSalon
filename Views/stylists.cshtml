<h2>All Stylists</h2>
@if (Model.Count == 0)
{
  <p>There are no stylists yet.</p>
}
<ul>
  @foreach (var stylist in Model)
  {
    <li><a href="/stylist/@stylist.GetId()">@stylist.GetName()</li>
  }
</ul>
<a href="/stylist/new">Add a new stylist</a>
<a href="/">Back to home</a>
